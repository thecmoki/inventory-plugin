<div style = "text-align: right; font-size: 0.9em;">
	<%= link_to "English", "#{categories_path}?lan=en" %> |
	<%= link_to "Albanian", "#{categories_path}?lan=al" %>
</div>
<div class = "contextual">
	<%= link_to(addCategory(session[:lan]), categories_new_path, :class => "icon icon-add") %>
</div>
<h2><%= categoryHeader(session[:lan]) %></h2>
<div class = "tabs">
	<ul>
		<li><%= link_to(goInventories(session[:lan]), inventories_path) %></li>
		<li><%= link_to(goUnits(session[:lan]), units_path) %></li>
		<li><%= link_to(goCategories(session[:lan]), categories_path, :onclick => "showTab('info', this.href); this.blur(); return false;", :id => "tab-info", :class => "selected") %></li>
		<li><%= link_to(goRooms(session[:lan]), rooms_path) %></li>
		<li><%= link_to(goAmortNorms(session[:lan]), amortnorms_path) %></li>
		<li><%= link_to(goHistories(session[:lan]), histories_path) %></li>
	</ul>
</div>
<div style = "width: 50%">
	<div class = "autoscroll">
		<table class = "list">
			<thead>
				<tr>
					<th>ID</th>
					<th><%= categoryName(session[:lan]) %></th>
					<th><%= categoryComment(session[:lan]) %></th>
					<th></th>
				</tr>
			</thead>
			<tbody>
				<% @categories.each do |category| %>
					<tr class = "<%= cycle('user active odd', 'user active even') -%> user">
						<td><%= category.id %></td>
						<td><%= category.name %></td>
						<td><%= category.comment %></td>
						<td class = "buttons">
							<%= link_to(editCategory(session[:lan]), categories_edit_path(:id => category.id), :class => "icon icon-edit") %>
							<%= link_to(deleteCategory(session[:lan]), categories_destroy_path(:id => category.id), :method => :delete, :class => "icon icon-del", data: dataConfirm(session[:lan])) %>
						</td>	
					</tr>
				<% end %>
			</tbody>
		</table>
	</div>
</div>
<% content_for :header_tags do %>
	<%= stylesheet_link_tag 'style', :plugin => 'regjistrimi_inventarit' %>
<% end %>