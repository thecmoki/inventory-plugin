<style type="text/css">
	.splitcontentleft fieldset input {
		width: 90%;
	}
	.splitcontentleft fieldset select {
		width: 90%;
	}
	.splitcontentleft textarea {
		width: 90%;
		height: 70px;
	}
</style>
<h2><%= amortnormPathNew(session[:lan]) %></h2>
<% if @amortnorm.errors.any? %>
	<div id = "errorExplanation">
		<ul>
			<% @amortnorm.errors.full_messages.each do |message| %>
				<li><%= message %></li>
			<% end %>
		</ul>
	</div>
<% end %>
<div class = "splitcontentleft">
	<%= form_for(@amortnorm) do |f| %>
		<fieldset class = "box tabular">
			<legend><%= amortnormLegend(session[:lan]) %></legend>
			<p>
				<%= f.label(amortnormName(session[:lan])) %>
				<span class = "required">*</span>
				<%= f.text_field(:name) %>
			</p>
			<p>
				<%= f.label(amortnormComment(session[:lan])) %>
				<%= f.text_area(:comment) %>
			</p>
		</fieldset>
		<p><%= f.submit(amortnormNewSubmit(session[:lan])) %></p>
	<% end %>
<div>