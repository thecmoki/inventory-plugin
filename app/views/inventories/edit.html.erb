<style type="text/css">
	.splitcontentleft fieldset input {
		width: 90%;
	}
	.splitcontentleft fieldset select {
		width: 90%;
	}
	.splitcontentleft textarea {
		width: 90%;
		height: 70px;
	}
</style>
<h2><%= link_to(l(:inventoryHeader), inventories_path) + " " + l(:inventoryPathEdit) %></h2>
<% if @inventory.errors.any? %>
	<div id = "errorExplanation">
		<ul>
			<% @inventory.errors.full_messages.each do |message| %>
				<li><%= message.capitalize %></li>
			<% end %>
		</ul>
	</div>
<% end %>
<div class = "splitcontentleft">
	<%= form_for(:inventory, :url => {:action => "update", :id => @inventory.id}) do |f| %>
		<fieldset class = "box tabular">
			<legend><%= l(:legend1) %></legend>
			<p>
				<%= f.label(l(:userName)) %>
				<span class = "required">*</span>
				<%= f.collection_select(:user_name, User.all, :id, :full_name, :prompt => "Select User")%>
			</p>
			<p>
				<%= f.label(l(:roomName)) %>
				<span class = "required">*</span>
				<%= f.collection_select(:room_name, Room.all, :name, :name, :prompt => "Select Room") %>
			</p>
			<p>
				<%= f.label(l(:productName)) %>
				<span class = "required">*</span>
				<%= f.collection_select(:product_name, Unit.all, :name, :name, :prompt => "Select Product") %>
			</p>
			<p>
				<%= f.label(l(:serialNumber)) %>
				<span class = "required">*</span>
				<%= f.text_field(:serial_number) %>
			</p>
			<p>
				<%= f.label(l(:productId)) %>
				<span class = "required">*</span>
				<%= f.text_field(:product_id) %>
			</p>
			<p>
				<%= f.label(l(:activationDate)) %>
				<span class = "required">*</span>
				<%= f.date_field(:activation_date) %>
			</p>
		</fieldset>
		<fieldset class = "box tabular">
			<legend><%= l(:legend2) %></legend>
			<p>
				<%= f.label(l(:buyDate)) %>
				<%= f.date_field(:buy_date) %>
			</p>
			<p>
				<%= f.label(l(:amortizationNorm)) %>
				<%= f.text_field(:amortization_norm) %>
			</p>
			<p>
				<%= f.label(l(:amortization)) %>
				<%= f.text_field(:amortization) %>
			</p>
			<p>
				<%= f.label(l(:netoValue)) %>
				<%= f.text_field(:neto_value) %>
			</p>
			<p>
				<%= f.label(l(:timeOfUse)) %>
				<%= f.text_field(:time_of_use) %>
			</p>
			<p>
				<%= f.label(l(:comment)) %>
				<%= f.text_area(:comment) %>
			</p>
		</fieldset>
			<%= f.submit(l(:editSubmit))%>
	<%end%>
</div>